# 固定表头与操作列实施概览

## 完成内容

- 统一固定 13 个“操作”列，覆盖：
  - 7 个业务主表：螺丝规格、冲头、牙板、皮带、主模具、剪刀、上冲。
  - 5 个关联表：冲头/牙板的螺丝规格关联，主模具/剪刀/上冲的线材关联。
  - 设置页备份记录表。
- 新增全局滚动同步逻辑 `src/composables/useFixedOperationColumns.ts`。
- 设置页备份表增加独立横向滚动容器，窄窗口下操作列不会被裁切。
- 操作列表头、正文、斑马纹、悬浮态均适配浅色和深色主题。
- 所有外层滚动表格统一固定表头，覆盖主业务表、记录/关联表、库存汇总和设置备份表。

## 关键技术决策

项目当前由外层容器负责横纵滚动，直接使用 vxe-table 的内部固定列或单纯 `position: sticky` 无法真正固定。最终方案按外层容器剩余横向滚动距离固定操作列，并按 `scrollTop` 固定表头，统一通过 GPU Transform 同步；保留现有 WebView2 兼容滚动架构，不重新引入内部滚动和表格裁切问题。

## 验证结果

- 7 个主表在横向滚动起点、中点、终点的操作列右边界一致。
- 7 个主表在纵向滚动起点、中点、终点的表头顶部位置一致。
- 记录表滚动到底后表头位置不变；关联表、库存表与设置备份表已纳入同一规则。
- 5 个关联表与设置页备份表的操作列验证通过。
- 纵向滚动到底后最后一行完整可见。
- 浅色、深色与斑马纹背景显示正常。
- Vue TypeScript：通过。
- Node TypeScript：通过。
- Vite 生产构建：通过。
- `git diff --check`：通过。

## 后续说明

本轮改动尚未提交或推送。工作区原有的 `anti-duplicate-design.md` 和 `overview.md` 仍保持未跟踪状态，本轮没有修改它们。
