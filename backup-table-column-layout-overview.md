# 备份记录表割列修复概览

## 问题根因

- 设置页备份表使用 `fit=false`，五列固定宽度合计 760px。
- 表格和内部 vxe 容器又被设置为 `width: max-content`，在较窄内容区必然产生横向溢出。
- 右侧操作列通过 Transform 固定后会覆盖锁定列的部分可视区域，形成截图中的列被切开、边界重叠效果。

## 修复内容

- 备份表改为 `fit=true`，让列按可用空间均衡分配。
- 五列改为 24% / 18% / 34% / 10% / 14%：
  - 备份时间 24%。
  - 备份原因 18%。
  - MD5 34%。
  - 锁定 10%。
  - 操作 14%。
- 为每列保留合理 `min-width`，整表最小宽度为 600px；窗口更窄时仍可使用外层横向滚动。
- 移除备份表的 `max-content` 强制撑宽，改为 `width: 100%`。
- 操作列使用独立类名；无横向溢出时回归普通网格边框，不再呈现独立浮层。
- 修复固定列同步器的边界条件：无真实横向溢出时不再减去 scrollbar gutter，操作列位移归零，避免左移约一个滚动条宽度后压住前一列。
- 只有发生真实横向溢出时才显示固定操作列阴影和增强分隔线。

## 验证结果

- Vue TypeScript 检查通过。
- Vite 生产构建通过：2185 个模块，最终构建耗时 1.42 秒。
- `git diff --check` 通过，仅保留既有的 LF/CRLF 行尾提示。

## 影响范围

- 仅调整设置页的备份记录表。
- 其他业务表格的固定操作列、滚动与列宽策略不受影响。
