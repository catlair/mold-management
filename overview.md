# 螺丝附件与标注功能实施概览

## 已完成

- 螺丝规格表新增附件数量列，以及操作列中的预览入口。
- 编辑螺丝规格时可进入附件管理工作区；新增规格会明确提示先保存再添加附件。
- 每条规格支持多个图片或 PDF，单文件限制 50MB。
- 编辑模式支持添加、切换、重命名、删除、画笔、矩形、箭头、文字、颜色、线宽、撤销和清除当前页。
- 预览模式支持图片/PDF 查看、缩放、PDF 翻页、标注显示开关和标注副本导出。
- 标注使用 0～1 归一化坐标独立保存，原始附件保持不变；PDF 标注按页保存。
- 可将图片或当前 PDF 页与标注合成为 PNG 副本，桌面端使用保存对话框，浏览器端直接下载。

## 存储与安全

- 附件保存在 Excel 数据文件同级的 `attachments/` 目录。
- `attachments/index.json` 保存元数据、排序和标注，不把二进制写入 Excel。
- Rust 后端校验扩展名、文件大小和存储路径，使用 UUID 文件名并防止目录穿越。
- 删除螺丝规格时同步清理它的附件，其他规格附件不受影响。
- 备份指纹覆盖 Excel 与附件目录；新备份创建同名附件快照，清理和恢复均同步处理。
- 恢复历史旧备份时若没有附件快照，会保留当前附件，避免误清空。

## 浏览器自测能力

- 浏览器 Mock 内置一张图片和一份两页 PDF，可验证预览、PDF 翻页和标注。
- 浏览器编辑模式支持真实文件输入，标注元数据写入本地存储，便于刷新后复测。
- 桌面环境继续通过 Tauri/Rust 执行真实文件导入和持久化。

## 验证结果

- Rust 编译通过。
- Rust 测试 5/5 通过，其中 2 项覆盖附件完整生命周期和按螺丝清理。
- Vue TypeScript 检查通过。
- Vite 生产构建通过，PDF.js Worker 已正确产出。
- `git diff --check` 通过。
- 浏览器自动化验证附件列、固定操作列、横纵滚动、图片预览、两页 PDF、编辑工具、标注保存、导出入口和深色主题；无页面控制台错误。

## 说明

- 本轮未提交、未推送、未启动桌面开发版。
- 工作区还包含此前未提交的固定表头、固定操作列，以及同期出现的页面表格配置改动。
