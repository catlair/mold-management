# 页面管理深色表格主题修复概览

## 完成内容

- 修复 `src/App.vue` 中 Element Plus 表格仍使用硬编码浅色表头、偶数行和悬停色的问题。
- 全局 Element Plus 表格改为复用应用主题变量：卡片表面、弱化表面、悬停表面、边框及主次文字颜色。
- 新增浅色和深色独立的表格斑马行变量，避免深色模式出现发白横条。
- 页面管理列配置表启用 `stripe`，并映射 `--el-fill-color-lighter`，确保斑马纹在两种主题下都可辨识但不过亮。
- 页面管理开关统一开启、关闭、禁用三种轨道颜色，并补充键盘焦点轮廓。

## 关键决策

- 不再使用 `#fafbfc`、`#303133`、`#f0f5ff` 等固定浅色值，而由 `:root` 与 `:root.dark` 的主题变量控制。
- 将基础 Element Plus 表格主题映射放在全局层，页面管理只保留该页特有的斑马纹和开关状态定制，减少后续同类深色回归。
- Hover 同时覆盖原生 `:hover` 与 Element Plus 维护的 `hover-row` 类，避免斑马行优先级压住悬停反馈。

## 验证结果

- 主题规则静态核验通过：10 项关键变量与选择器均存在。
- Vue TypeScript 检查通过。
- Vite 生产构建通过：2185 个模块，构建耗时 1.78 秒。
- `git diff --check` 通过；只有既有 `DataTable.vue` 的 LF/CRLF 行尾提示。

## 说明

- 当前环境未找到可执行的 Chrome/Edge，且浏览器自动化命令不可用，因此本轮未完成实际浏览器计算样式与截图测量；代码已通过 HMR 开发服务器、类型检查和生产构建验证。
- 本轮未提交、未推送；工作区仍包含页面管理、附件、固定表头和固定操作列等此前未提交改动。
